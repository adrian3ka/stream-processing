package beam.example.basic;

import org.apache.beam.sdk.io.gcp.pubsub.PubsubClient;
import org.apache.beam.sdk.io.gcp.pubsub.PubsubIO;
import org.apache.beam.sdk.io.gcp.pubsub.PubsubJsonClient;
import org.apache.beam.sdk.io.gcp.pubsub.PubsubMessage;
import org.apache.beam.sdk.options.PipelineOptionsFactory;

public class StreamingWordcap {

  /*public static void main(String[] args) {

    PubsubJsonClient pubsubJsonClient = PubsubJsonClient.FACTORY.newClient(

    );
    PubsubJsonClientFactory
    PubsubClient.PubsubClientFactory


 PubsubMessage pubsubMessage -


    PubsubMessage pubsubMessage = new PubsubMessage()
    PubsubIO.writeMessages()

    WordCountOptions options =
      PipelineOptionsFactory.fromArgs(args).withValidation().as(WordCountOptions.class);

    //runDebuggingWordCount(options);
  }*/
}
